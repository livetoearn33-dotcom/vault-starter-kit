# {{知識庫名稱}}【要填】 工作規則

> 【這個引言區塊在寫進 vault 時整段刪掉，它只是給填表的人看的】
> 這份檔案放在 vault 根目錄，是寫給 AI 看的規則書。
> 任何 AI 在這個資料夾工作前先讀它，然後照規矩辦事。
> 佔位有兩種：`{{...}}【要填】` 換成使用者的實際答案；`{{...}}【示意】` 是格式示範，換成一個具體例子。
> 兩種都不能留著括號進 vault。

這是 {{使用者稱呼，例：我}}【要填】 的個人知識庫與工作系統，不是程式專案。
最重要的原則：**筆記歸對位置、命名一致、不動人手寫的內容**。

## 三層各司其職

- **資料夾路由表**管「放哪」：存東西時照下表歸位
- **主題索引頁**（`10-知識庫/主題/`）管「找什麼」：AI 檢索用，由 AI 定期編譯維護。**目前未啟用**（見下）
- **全文搜尋**兜底：前兩層找不到時用

資料夾沒有退化成暫存區，它仍然是第一層路由。

**主題索引頁的啟用門檻：全庫筆記滿 30 篇。**

在那之前 `10-知識庫/主題/` 這個資料夾不存在，AI 不要建它、不要往裡面寫、不要在筆記裡連到它。筆記的 `entities` 一律寫 `[]` 加 `entity_confidence: none`。理由：東西不夠多的時候，資料夾加全文搜尋就找得到，硬建主題頁只會做出一堆空頁，然後每週對著空頁做維護。

滿 30 篇之後，由每週自動任務提案新主題（寫進 `00-總覽/主題候選.md`），人審過才成立。AI 任何時候都不能自行建主題頁。

## 資料夾路由（新筆記存哪裡）

| 內容類型 | 存放位置 |
|---|---|
| 外部文章、剪報、靈感 | 判得出所屬專案 → `20-{{專案或品牌}}【要填】/<專案名>/` 根層；判不出來 → `10-知識庫/raw-未分類/` |
| 別人的方法論原文整理 | `10-知識庫/外部參考/` |
| 我們自己怎麼做（工具教學、SOP） | `10-知識庫/工具與SOP/` |
| AI 編譯的主題索引頁 | `10-知識庫/主題/`（**未啟用，全庫滿 30 篇後才開**；查主題用，新筆記不要放這裡） |
| 跟 AI 對話得出的複雜結論 | `10-知識庫/AI問答歸檔/`（見下方「AI 問答落檔」） |
| 各專案的產出 | `20-{{專案或品牌}}【要填】/<專案名>/`，實際資料夾名：{{逐一列出他實際有的專案，兩個就寫兩個，五個就寫五個}}【要填】（照這些名字用，不要另建大小寫不同的新資料夾） |
| {{多人共用的文件}}【要填｜一個人用就把整列刪掉】 | `50-共用/`（這區{{夥伴稱呼}}【要填】全看得到，個人內容與密碼金鑰絕不放這裡） |
| 會議紀錄、逐字稿、客戶或合作方來信 | 判得出所屬專案 → `20-{{專案或品牌}}【要填】/<專案名>/`；判不出來 → `10-知識庫/raw-未分類/` |
| ⛔ 私密內容（{{這區實際裝什麼，例：健康、感情、薪水／或：客戶合約、報價、未公開計畫}}【要填】） | `40-個人/`。資料夾名固定叫 `40-個人`，不管裡面裝的是個人隱私還是工作機密。判斷法：內容出現{{三到五個判斷關鍵詞}}【要填】，一律是私密，不管它掛哪個專案 |
| 過時內容 | `99-封存/`（移入前先問人） |
| 分不出類的 | `10-知識庫/raw-未分類/`（永久的家：不再搬家，靠全文搜尋找得到，主題頁啟用後再補標記；不要硬猜專案） |

## AI 查資料時（找素材的順序）

1. 全域搜尋關鍵字
2. 按上面路由表翻對應資料夾
3. （主題索引頁啟用後才有這一步）開 `10-知識庫/主題/{主題}.md` 看反向連結

## AI 禁寫區與工作區

| 區域 | AI 的權限 |
|---|---|
| `10-知識庫/raw-未分類/`、`10-知識庫/AI問答歸檔/`（主題頁啟用後加上 `10-知識庫/主題/`） | **工作區**：可以直接寫、直接改。主題頁是例外：內容可以編譯維護，但**新開一頁一定要人審**，AI 不能自行建 |
| `20-{{專案或品牌}}【要填】/`、`10-知識庫/外部參考/`、`10-知識庫/工具與SOP/` | **可新增，不可改舊**：新檔隨便建，既有檔案要改先問 |
| `40-個人/` | **完全禁止**：不讀、不寫、不統計 |
| `99-封存/` | **唯讀**：搬東西進去一定要先問 |
| `.obsidian/` | **完全禁止** |

理由一句話：AI 產出的速度一旦超過人裁決的速度，這個知識庫就不再代表「我的判斷」，會變成「我和 AI 的混合物」。禁寫區是防線。

## Frontmatter schema

每篇筆記開頭的四欄：

```yaml
---
title: "10-20 字精煉標題"
date: {{YYYY-MM-DD}}【示意｜換成今天日期】
{{專案欄位名，例：project}}【要填】: {{專案代號}}【示意】    # 沒有歸屬就省略這欄，不要寫 null
entities: []                                 # 主題頁啟用前一律空陣列，並補 entity_confidence: none
---
```

選填：`tags`、`entity_confidence`（high / medium / low / none）。

**`source_url` 對外部素材是必填**：剪報、外部文章整理、外部參考、raw-未分類 都算。沒有來源的剪藏等於沒剪。查不到來源 → 一律存 `10-知識庫/raw-未分類/` 並標 `entity_confidence: none`，不要硬猜專案。人手寫的反思、方法論、產品文件不受此限。

欄位只留這幾個。想加新欄位前先問：三個月後有人會用它做篩選嗎？答不出來就別加。

## 命名慣例

- 有日期的筆記：`YYYY-MM-DD_主題.md`（例：`{{YYYY-MM-DD}}【示意】_某某工具使用心得.md`）
- 專案文件：`專案名-文件類型-日期或版本.md`（例：`某專案-設計規範-v0.2.md`）
- 方法論類：`_方法論_主題.md`，開頭加底線置頂

## 「我的判斷」區塊

每一篇存進來的素材，正文結尾都要有這一段：

```markdown
## 我的判斷

- **為什麼留**：（一句話，這篇對我有什麼用）
- **下一步**：（打算拿它做什麼；還不知道就寫「暫存」）
- **相關**：[[某篇筆記]]（還沒有相關的就寫「暫時沒有」，不要硬連；主題頁啟用後才連得到主題）
```

**為什麼這件事重要**：AI 從知識庫讀到的如果只是網路上本來就查得到的內容，這個庫對 AI 就沒有獨特價值。有人的判斷在裡面，AI 才寫得出只有這個人寫得出的東西。這是從「資料倉庫」變成「第二大腦」的分界。

執行規則：

- **AI 存素材時**：能從對話上下文推得出「為什麼留」就先寫進去，並在回報時說「判斷欄我先擬了一句，你要改就改」
- 上下文推不出來 → 寫 `- **為什麼留**：（待補）`，**不要編一句聽起來很有道理的話**
- 舊筆記不回頭補；哪天真的用到某篇舊筆記，再順手補那一篇

## AI 問答落檔

跟 AI 對話時，只要得出「下次還會用到」的結論（設計方案、比較結果、對知識庫的複雜提問答案），就落一檔到 `10-知識庫/AI問答歸檔/`，不要留在聊天視窗。

- 檔名：`YYYY-MM-DD_問題一句話.md`
- 正文固定四段：**問題**、**結論**（先講）、**依據**（引用的檔案用 `[[連結]]`）、**不確定處**（沒查證的明寫）
- 結尾照樣加「## 我的判斷」
- 這個資料夾是 AI 的工作區：AI 可以直接寫，人只勾「我的判斷」；被人認證過、值得長期留的結論，再由人決定搬去對應的專案或知識庫資料夾

## 每週健康檢查三個數字

每週自動任務算三個數字，寫進 `00-總覽/健康檢查.md`：

- **孤兒**：正文沒有任何 `[[連結]]`、frontmatter 的 `entities` 也是空的實質筆記。**主題索引頁啟用前，這個數字會等於筆記總數，屬正常，先不用管**
- **缺來源**：外部素材類沒有 `source_url` 的筆記
- **未認證**：近 30 天新增、但正文沒有「## 我的判斷」的筆記

數字只回報，不自動清理。清理一律等人決定。

## 私密區鐵則

`40-個人/` 整個資料夾是私密區，對 AI 的鐵則：

- **永不外流**：`40-個人/` 的任何內容，不得出現在任何對外或共用輸出——{{對外輸出的例子，例：貼文、提案、電子報}}【要填】、`50-共用/`（沒這區就刪掉）、主題索引頁、`00-總覽/` 的彙整。直接引用、改寫、「拿來當靈感素材」都算外流。
- **編譯豁免**：AI 編譯主題頁、算健康檢查、做任何彙整報告時，一律跳過 `40-個人/`。
- **對話門檻**：只有人在當下對話中主動開啟相關主題時，才可談論其內容；其他任務中不主動提起、不順帶引用。
- **不搬不抄**：永不移動或複製 `40-個人/` 的檔案到其他資料夾。
- frontmatter 標了 `private: true` 的筆記，無論放在哪個資料夾，一律適用以上規則。

## 禁止事項

- 不要改 `.obsidian/` 裡的任何檔案
- 不要主動移動、改名、刪除既有筆記；人說「整理」也要先列清單給他確認
- 不要主動 git commit、push、建分支
- 筆記內部連結用 Obsidian 的 `[[檔名]]` 格式，不要用 markdown 相對路徑
- 通用 skill 或外掛的範例慣例，一律讓位給本檔規則：不要因為某個範例有多餘欄位就自行加入
- 遇到本檔沒寫到的情況，先問，不要自行發明規則。問完的答案補進本檔

## 有人說「存入知識庫」時

1. 按上表選資料夾，按命名慣例取檔名
2. 開頭加一段 2-3 行的摘要（這則筆記是什麼、為什麼存）
3. **正文結尾加「## 我的判斷」區塊**
4. 存完回報完整路徑，讓人可以點開確認

## 等我決定的事

任何 AI 想做又不敢決定的事（要不要刪、要不要改結構、兩個方案選哪個），一律寫進 `00-總覽/懸空件隊列.md` 的「待辦」區，附一句說明與預設選項。不要只在聊天視窗講一聲就算了。
