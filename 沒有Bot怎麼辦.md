# 沒有 Bot 怎麼辦

我們這套系統有一塊你複製不了：一支 Telegram Bot。在手機上看到好東西，貼給 Bot 一秒進庫，AI 自動判斷放哪、補好欄位；產出的內容跑完之後，成效數據也自動回流到庫裡。

這一塊是自己寫的程式，這包沒有附。這頁講的是沒有 Bot 的話怎麼補，以及補得起來多少。

先給結論：**用 Obsidian Web Clipper 加 Claude Code，能做到八成。** 剩下兩成是「手機上一秒存入」跟「成效自動回流」——其中手機那一半可以自己補回來（Obsidian 官方 App 加 Git 插件就能在手機上存，做法見 `雲端同步與手機.md`），差的只是速度；成效自動回流那一半真的需要一支 Bot。

## 三個替代方案

### 一、Obsidian Web Clipper（取代電腦上的剪藏）

官方出的瀏覽器擴充功能，Chrome 與 Safari 都有，免費。它負責把網頁變成一篇合格的筆記。

裝好之後，做一件關鍵設定：**新增一個 template，強制帶入 `source_url`。**

在 Clipper 的 Settings → Templates 新增一個範本，properties 設成：

| 欄位 | 值 |
|---|---|
| `title` | `{{title}}` |
| `date` | `{{date}}` |
| `source_url` | `{{url}}` |
| `entities` | 留空，讓 AI 之後補 |
| `entity_confidence` | `none` |

存檔位置設成 `10-知識庫/raw-未分類/`，檔名格式設成 `{{date}}_{{title}}`。

為什麼一定要 `source_url`：沒有來源的剪藏等於沒剪。三個月後你看到一段結論，想確認它是誰說的、在什麼脈絡下說的，沒有網址就只能重新 Google，那篇筆記等於作廢。健康檢查的「缺來源」數字就是在抓這個。

剪完全部先進 `raw-未分類/`，不要在剪的當下逼自己分類。分類交給下一步。

### 二、Claude Code 批次歸檔（取代 Bot 的自動判斷）

Bot 是即時歸檔，你改成一週歸一次。累積一批之後打開 Claude Code，貼這段：

```
讀 vault 根目錄的 CLAUDE.md，然後處理 10-知識庫/raw-未分類/ 裡
最近新增、還沒有 entities 欄位的筆記（最多 10 篇）：

1. 讀每篇的內容，依 CLAUDE.md 的路由表判斷它該去哪個資料夾
2. 判得出所屬專案的：搬過去，並補上 frontmatter 四欄
3. 判不出來的：留在 raw-未分類，標 entities: [] 加 entity_confidence: none
4. 每一篇在結尾補「## 我的判斷」三行；推不出「為什麼留」就寫（待補），不要編
5. 開頭補兩到三行摘要

做之前先列清單給我看你打算怎麼分，我說可以再動手。正文一個字都不要改。
```

最後那句「先列清單給我看」很重要。分類這件事你只要看一眼就知道對不對，但改錯了很難發現。

一週跑一次，十篇大概三分鐘。跟 Bot 的差別只在延遲，不在品質——判斷是同一個 AI 做的，讀的是同一份 `CLAUDE.md`。

### 三、手機用官方 App 加一個 inbox 資料夾（取代手機一秒存入）

這是最將就的一塊，講清楚：手機上不會有 Bot 那麼順。

做法：

1. 手機裝 Obsidian 官方 App，並把手機接上你的 vault。**做法看 `雲端同步與手機.md`**：用 GitHub 私有 repo 加 Obsidian 的 Git 插件，免費，而且電腦端那套版本紀錄也一起拿到（AI 改壞檔案可以退回去）。不想碰 Git 的話，官方 Obsidian Sync 付費版最省事，iCloud 免費但沒有版本紀錄。
2. 在 `00-總覽/` 底下開一個 `inbox/` 資料夾（完整路徑 `00-總覽/inbox/`），並在 `CLAUDE.md` 的路由表補一列，寫明它只是暫存、每週會被清空。
3. 在 Obsidian App 設定裡把「新筆記位置」指到 `inbox/`。
4. 手機上看到東西，開 App 建一則新筆記，貼上內容加網址，不用管格式，不用管標題。
5. 每週跑上面那段批次歸檔時，把 `inbox/` 一起處理掉。

iPhone 使用者可以再加一層：用捷徑（Shortcuts）做一個「加到 inbox」的分享選單動作，把選取的文字加網址附加到一個固定檔案。設定一次大概十分鐘，之後從任何 App 分享兩下就進得去。

實話：這個流程從「看到」到「存好」大概要 15 秒，Bot 是 3 秒。差的那 12 秒會讓你少存一些東西，這是沒有 Bot 的實際成本。

還有一點要先知道：手機版的 Git 插件不是真正的 git，vault 大的話會慢、偶爾會閃退。細節與排錯都寫在 `雲端同步與手機.md`。

## 這樣補完，還差什麼

| 功能 | Bot | 替代方案 | 差多少 |
|---|---|---|---|
| 電腦剪網頁 | 一鍵 | Web Clipper 一鍵 | 沒差 |
| 自動判斷放哪、補欄位 | 即時 | Claude Code 每週批次 | 只差延遲 |
| 手機存入 | 3 秒 | App 加 Git 同步、丟進 inbox，15 秒 | 有感，會少存一些 |
| 產出成效自動回流 | 有 | 沒有，要手動記 | 差最多，短期用不到 |
| 人機判斷差異紀錄 | 有 | 沒有 | 這是進階功能，前三個月用不到 |

前三個月你會用到的只有前三列，替代方案都夠用。後兩列是系統跑順之後才會想要的東西，那時候再說。

## 想要那兩成的話

如果你跑了兩三個月，確定這套對你有用，也真的被那 12 秒卡到了，Bot 那部分可以找 Zeno 聊。（聯絡方式待補）
