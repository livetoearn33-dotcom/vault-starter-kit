# 雲端同步與手機

你的 vault 現在只活在一台電腦的一個資料夾裡。這頁教你把它接上雲端，並讓手機也能寫。

做法是 Git：電腦上的筆記照常是本地檔案（Obsidian 開起來一樣快），背景每隔幾分鐘自動把改動送上雲端；手機透過同一個雲端來回收送。全程免費，只要一個 GitHub 帳號。

不用懂 Git 是什麼，照著做就會動。指令都可以直接複製。

---

## 為什麼要做

**一、備份。** 筆記存在你的硬碟裡，硬碟壞掉、電腦掉了、資料夾誤刪，全部消失。接上雲端之後，遠端隨時有一份完整副本。

**二、手機寫、電腦收。** 在捷運上想到一句話，用手機打進 Obsidian；回到電腦打開，它已經在那裡了。反過來也一樣。

**三、AI 改的東西有版本可以退。** 這套系統的前提是 AI 會幫你搬檔、改欄位、批次整理。它偶爾會改壞。有 Git 的話，每一次改動都留有紀錄，你可以叫 AI「把某某檔還原成昨天的樣子」，它做得到。沒有 Git 的話，改壞了就是改壞了。

第三點是這頁真正的重點。你越信任 AI 動你的檔案，就越需要一條退路。

**做完的樣子**：你講得出這三個好處各是什麼，而且知道第三點跟你每天在做的事直接相關。

---

## 一次性設定：電腦端

四十分鐘，做一次就好。

### 第 1 步：確認電腦有 git

打開終端機，輸入：

```
git --version
```

有跳出版本號（像 `git version 2.39.0`）就是有了，跳過這步。

沒有的話：

- **Mac**：輸入 `xcode-select --install`，跳出視窗按安裝，等它跑完。
- **Windows**：到 `https://git-scm.com/download/win` 下載安裝，一路按下一步就好。

裝好之後告訴 git 你是誰（這只是用來標記每次改動是誰做的，隨便填也可以）：

```
git config --global user.name "你的名字"
git config --global user.email "你的email"
```

### 第 2 步：開一個 GitHub 私有 repo

1. 到 `https://github.com` 註冊帳號（免費）。
2. 右上角 `+` → New repository。
3. Repository name 隨便取，例如 `my-vault`。
4. **Visibility 一定要選 Private。** 這是你的筆記，裡面會有客戶名、還沒發布的稿子、私人反思。選 Public 等於公開發表。
5. 下面的 Add a README、.gitignore、license 全部不要勾，保持空的。
6. 按 Create repository。

建完那一頁會給你一個網址，長得像 `https://github.com/你的帳號/my-vault.git`。**把它複製下來貼在記事本，等一下要用兩次（電腦一次、手機一次）。**

### 第 3 步：Obsidian 裝 Git 插件

在 Obsidian 裡：Settings → Community plugins → 如果看到 Restricted mode，先按 Turn on community plugins → Browse → 搜尋 `Git` → 找到作者是 Vinzent03 的那個 → Install → Enable。

### 第 4 步：六個設定值照抄

回到 Settings，左側最下面會多出一個 `Git`，點進去。找到這六項，照下表設：

| 設定項 | 設成 | 這是在幹嘛 |
|---|---|---|
| Auto commit-and-sync interval | `10`（分鐘） | 每 10 分鐘自動把你的改動送上雲端 |
| Auto pull interval | `5`（分鐘） | 每 5 分鐘去雲端看有沒有別台裝置寫了新東西 |
| Pull on startup | 開 | 每次打開 Obsidian 先抓最新的下來 |
| Pull on commit-and-sync | 開 | 送出去之前先抓一次，避免撞車 |
| 合併策略（merge / rebase 那一項） | `merge` | 兩邊都有改動時怎麼合。選 merge，不要選 rebase（手機端不支援 rebase） |
| Commit message | `vault backup: {{date}}` | 每次自動存檔的標題，會自動帶入時間 |

設定項名稱在不同版本會微調（例如舊版把「Pull on commit-and-sync」叫「Pull before push」，把 commit-and-sync 叫 backup）。**以你插件內設定頁實際顯示的名稱為準**，看到意思一樣的就是那一項。

### 第 5 步：放一份 `.gitignore`

有些檔案不該上傳：系統產生的垃圾檔、Obsidian 記錄「你上次開哪幾個分頁」的暫存檔（這種檔案每台裝置都不一樣，同步它只會一直吵架）。

在 vault 根目錄建一個檔案叫 `.gitignore`，內容貼這段：

```
.DS_Store
._*
.trash/
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
```

注意：這裡只排除 Obsidian 的**視窗狀態**，`.obsidian/` 底下的設定本身（插件清單、你的偏好）還是會備份，換電腦時可以直接還原。

最省事的做法：直接跟 Claude Code 說「幫我在 vault 根目錄建一個 `.gitignore`，內容是這幾行」，把上面那段貼給它。

### 第 6 步：第一次送上雲端

終端機切到你的 vault 資料夾，依序輸入這五行（第四行把網址換成你剛剛複製的那個）：

```
git init
git add .
git commit -m "vault backup: first"
git remote add origin https://github.com/你的帳號/my-vault.git
git push -u origin main
```

會跳出要你登入 GitHub 的視窗，照著登入就好（Mac 通常會開瀏覽器；Windows 會跳出登入視窗）。

如果最後一行報錯說 `main` 不存在，改成：

```
git branch -M main
git push -u origin main
```

**做完的樣子**
- 到 GitHub 網頁上打開你那個 repo，看得到你的資料夾跟筆記。
- 右上角確認寫著 `Private`。
- 回 Obsidian 隨便改一篇筆記，等十分鐘，重新整理 GitHub 網頁，看得到那次改動。
- Obsidian 左下角或狀態列會顯示 Git 的同步狀態（不同版本位置不一樣）。

---

## 一次性設定：手機端

三十分鐘。**先講實話**：手機端的 Git 插件不是用真正的 git，是用一套 JavaScript 重寫的版本（isomorphic-git），所以會慢、吃記憶體，vault 越大越容易卡。官方文件自己也寫明手機端不算穩定，可能在 clone 或 pull 時讓 Obsidian 閃退。

所以建議：**手機端等你電腦端跑順了、真的有「不在電腦前但想記東西」的需求再做。** 不是必要步驟。

### 第 1 步：申請一把 GitHub token

手機上沒辦法跳出瀏覽器登入，得先在電腦上申請一組「代替密碼的鑰匙」。

1. 電腦瀏覽器登入 GitHub → 右上角頭像 → Settings → 最下面 Developer settings。
2. Personal access tokens → **Fine-grained tokens** → Generate new token。
3. Token name 隨便取（例如 `obsidian-phone`）；Expiration 選一年。
4. Repository access 選 `Only select repositories`，勾你剛剛建的那個 vault repo。
5. Permissions → Repository permissions 底下開這幾項：
   - `Contents`：Read and write
   - `Metadata`：Read-only（通常會自動勾起來，不能取消）
   - `Commit statuses`：Read and write
6. Generate token，**畫面會出現一長串 `github_pat_` 開頭的字，只出現這一次**，馬上用你的密碼管理器存起來，或用能加密的方式傳到手機。

（有些教學會叫你用舊式的 classic token 加 `repo` 權限，那也能動，但權限開太大。能用 fine-grained 就用 fine-grained。）

### 第 2 步：手機裝 Obsidian 與插件

1. App Store / Play 商店裝 Obsidian 官方 App（免費）。
2. 打開，建一個**全新的空 vault**，名字隨便取。iPhone 上建議建在 App 自己的空間、不要放 iCloud（iCloud 跟 Git 同時管同一批檔案會打架）。
3. Settings → Community plugins → Turn on → Browse → 搜 `Git` → Install → Enable。

### 第 3 步：填帳號密碼、把筆記抓下來

1. Settings → Git → 找到 `Authentication/Commit Author` 那一區。
2. Username 填你的 GitHub 帳號，Password/Token 貼剛剛那串 token。
3. **同一區把 Author name 與 Author email 也填掉**（隨便填都行）。這欄空著的話，之後所有操作都會報「Git author information is not set」，這是手機端最常見的卡點。
4. 退出設定，打開命令面板（畫面右下角或上方的指令圖示），搜尋 `Clone existing remote repo`，點下去。
5. 貼上你的 repo 網址（結尾要有 `.git`），等它跑完。vault 大的話可能要好幾分鐘，中途不要切走。

如果 clone 一直失敗或閃退，通常是 vault 太大。這種情況先別硬試第三次，改用下面的替代方案。

### 第 4 步：以後每次開 App 先 pull

手機端不要開自動同步（會一直背景跑、耗電又容易出事）。養成兩個手動動作：

- 開 App 第一件事：命令面板 → `Pull`
- 寫完要關掉之前：命令面板 → `Commit-and-sync`

這兩個指令建議加到 Obsidian 手機版的工具列（Settings → Mobile → 管理工具列按鈕），變成兩個按鈕，會順很多。

### 不想用插件的替代方案

| 方案 | 費用 | 一句話 |
|---|---|---|
| Git 插件（本頁教的） | 免費 | 跟 AI 工作流同一套版本紀錄，改壞了能退；手機端偶爾要哄 |
| Obsidian Sync（官方付費） | 月費制 | 最穩、最快、支援衝突還原，但版本紀錄跟 AI 那套是分開的兩件事 |
| iCloud（Apple 裝置） | 免費 | 設定最簡單，但沒有版本紀錄，兩邊同時改容易產生「衝突副本」檔 |

**這頁主推 Git**，因為它免費、而且跟你用 Claude Code 的工作流是同一套東西——AI 改壞檔案時，救援指令跟同步是同一套機制。你如果純粹只要同步、完全不打算讓 AI 動你的檔案，官方 Sync 比較省事。

**做完的樣子**
- 手機 Obsidian 打得開你的 vault，看得到電腦上那些資料夾。
- 在手機上新建一篇筆記，跑 `Commit-and-sync`，回電腦打開 Obsidian，那篇筆記出現了。
- 工具列上有 Pull 與 Commit-and-sync 兩顆按鈕。

---

## 日常怎麼用

只有兩個場景，記住就好。

**手機記 → 電腦收**：手機隨手寫一篇，關掉之前按 `Commit-and-sync`。回到電腦打開 Obsidian，Pull on startup 會自動把它抓下來，你什麼都不用做。

**電腦改 → 手機看**：電腦上照常寫，每 10 分鐘自動送上去。要出門前想確定有送出去，在 Obsidian 命令面板跑一次 `Commit-and-sync`。手機打開 App 之後**先按 Pull**，再開始寫。

一句話版本：**電腦端全自動，手機端手動、開場先 pull。**

如果你跟別人共用同一個 vault（例如合作夥伴），多三條規矩：

1. 要改對方建立的檔案之前，先同步一次。
2. 準備大改某一份文件時，在檔頭留一行「2026-09-08 某某 編輯中」。
3. 每週對帳一次，看有沒有兩邊各做了一半的東西。

**做完的樣子**：連續三天不用想同步這件事，東西自己在兩邊出現。

---

## 衝突怎麼辦

衝突就是同一個檔案，兩台裝置各改了一版，Git 不知道該聽誰的。三句話處理：

1. **開始寫之前先 pull。** 九成的衝突是因為沒 pull 就開始改。
2. **同一份檔案，不要同時在兩台裝置大改。** 手機拿來記新東西、寫短的；長文件回電腦寫。
3. **真的撞到了**：檔案裡會出現 `<<<<<<<`、`=======`、`>>>>>>>` 三行標記，把兩個版本並排放著。不要慌，也不要亂刪——你自己挑要留哪一段，把三行標記刪掉就好。看不懂的話直接把整篇貼給 Claude Code 說「這是 git 衝突，幫我看兩版差在哪」，它會告訴你差別在哪、建議留哪一版。

**做完的樣子**：你看到 `<<<<<<<` 那幾行的時候知道那是什麼，不會以為檔案壞了。

---

## 安全三條

**一、repo 一定是 Private。** 建立時選錯的話，到 repo 的 Settings → 最下面 Danger Zone → Change visibility 改回 Private。改之前先想一下：如果它曾經公開過，就當作已經被看過，裡面的敏感內容要重新檢視。

**二、token 不要貼進筆記。** 那串 `github_pat_` 開頭的字等於你的帳號密碼。存在密碼管理器裡，不要存成一則 Obsidian 筆記——因為那篇筆記會被同步上雲端。

**三、任何 API key、密碼、金鑰，永遠不進 vault。** vault 會整包上雲端，這是原則問題不是機率問題。你如果不小心把 key 存進去、而且已經 push 上去了，光是刪掉檔案沒有用（歷史紀錄裡還在），正確做法是**去那個服務的後台把那把 key 作廢、重新申請一把**。

**做完的樣子**：GitHub 上你的 repo 顯示 Private；`grep -ri "api_key\|sk-\|github_pat" .` 在 vault 裡跑一次，沒有命中真的金鑰。

---

## 常見錯誤五條

**一、「Git author information is not set」（手機端最常見）**
手機端沒填 Author name 跟 Author email。回 Settings → Git → Authentication/Commit Author，兩欄都填上，隨便填也可以。

**二、push 被拒絕，說 `rejected` 或 `non-fast-forward`**
雲端有你本機還沒有的東西。先 pull 再 push。指令：`git pull` 然後 `git push`。插件裡就是先跑 Pull、再跑 Commit-and-sync。

**三、手機 clone 到一半閃退**
vault 太大，手機記憶體不夠。三個做法：關掉其他 App 再試一次（只試一次）；把大檔案（PDF、影片、大量圖片）從 vault 移出去再試；或者放棄手機端 clone，改用官方 Sync。不要連試五次，那是在浪費時間。

**四、Obsidian 一直跳出「有檔案被外部修改」的提示**
正常的，那就是 Git 把雲端的改動拉下來了。如果它跳得太頻繁到干擾你，把 Auto pull interval 從 5 分鐘拉長到 15 分鐘。

**五、明明改了東西，GitHub 上卻沒看到**
三個地方檢查：（a）等超過 10 分鐘了嗎；（b）那個檔案是不是被 `.gitignore` 排除了；（c）Obsidian 是不是關掉了——插件只在 Obsidian 開著的時候會自動跑。要立刻送出就手動跑一次 Commit-and-sync。

**做完的樣子**：這五條你掃過一遍，之後撞到時知道回來查。

---

## 參考

以下設定名稱與手機端流程，是照本文寫作當下（2026-09）的插件文件與社群紀錄整理的。**插件版本更新後名稱可能微調，一律以你插件內設定頁實際顯示的為準。**

- Obsidian Git 官方文件（Getting Started / Authentication）：`https://publish.obsidian.md/git-doc`
- 插件原始碼與 changelog：`https://github.com/Vinzent03/obsidian-git`
- 手機端限制（不支援 SSH、不支援 rebase、記憶體上限、可能閃退）：出自上述官方文件的 Mobile 說明
- 手機端 Author information 報錯：`https://github.com/denolehov/obsidian-git/issues/663`
- Fine-grained token 權限（Contents 讀寫、Metadata 唯讀、Commit statuses 讀寫）：出自 GitHub token 設定頁與社群設定紀錄，實際以你申請時看到的選項為準
